export interface IExperienceEntry {
  id: string;
  title: string;
  company: string;
  period: string;
  description: string;
}

export interface ICertificate {
  id: string;
  name: string;
  issuer: string;
  year: string;
  link: string;
}
